apiVersion: v1
kind: Service
metadata:
  name: zoujielun
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "zoujielun"
    port: 8080
  selector:
    app: zoujielun
